"""
Shared, reusable helpers for ModelLens analyses.

These are architecture-agnostic utilities (activation-shape handling, layer
resolution) factored out of individual analysis modules so they can be reused
rather than re-implemented per analysis.
"""
